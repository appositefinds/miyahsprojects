Flask==2.2.3
openai==0.28.0
gunicorn==20.1.0  # For running the app in production on platforms like Heroku or Render
Werkzeug==2.2.3   # Ensure compatibility with Flask
requests==2.31.0  # For making HTTP requests, might be required by OpenAI API
itsdangerous==2.1.2  # Used internally by Flask for session management
Jinja2==3.1.2     # Flask uses Jinja2 for templating
